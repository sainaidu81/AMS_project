import AppRoutes from "./routes/AppRoutes";

/**
 * Renders the application's route tree.
 *
 * @returns {JSX.Element} the root frontend component
 */
function App() {
  return <AppRoutes />;
}

export default App;
